/**
 * @module Config
 * @description This is a module, that holds all the settings for entire project
 * @author Oliwier Angielski
 */

enum Colors {
    BROWN = "brown",
    YELLOW = "yellow",
    BLUE = "blue",
    BLACK = "black"
}

enum Settings {
    WIDTH = 640,
    HEIGHT = 384,
    COLUMNS = 8,
    ROWS = 16,
    CELLSIZE = 16
}

export { Colors, Settings }