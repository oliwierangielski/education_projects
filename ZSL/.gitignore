# Node modules (common for Node.js, React, Express, React Native, WebGL)
node_modules/

# NPM and Yarn log files
npm-debug.log
yarn-debug.log
yarn-error.log

# Build directories (common for React, React Native, Express, WebGL)
build/
dist/
coverage/
*.tgz

# React Native specific
# Ignore android and ios builds
android/
ios/
*.xcodeproj
*.xcworkspace

# React Native Packager (metro bundler) cache
.react-native/
*.metro-*

# Expo files (if using Expo for React Native)
.expo/
.expo-shared/

# Environment files (common for all)
.env
.env.local
.env.*.local

# System files
.DS_Store
Thumbs.db

# Logs
*.log

# IDE files
.vscode/
.idea/

# macOS system files
Desktop.ini
$RECYCLE.BIN/

# Xcode specific (Swift)
# Ignore build folder and other Xcode files
*.xcuserstate
*.xcworkspace
*.xcodeproj
build/

# Swift specific (Xcode projects)
# Ignore Swift build artifacts
*.swiftpm/
Packages/

# Package-lock.json and yarn.lock
# It’s generally a good idea to track these, but you can ignore if you don’t want them
package-lock.json
yarn.lock

# WebGL specific
# Ignore build output for WebGL (e.g., Webpack, Three.js, or other related tools)
*.glb
*.gltf
*.bin
*.json
*.obj
*.mtl

# If using tools like Webpack for WebGL bundling
/dist/
/public/

# Temporary files created by WebGL or 3D rendering engines (e.g., Three.js)
*.bak
*.tmp
*.swp
